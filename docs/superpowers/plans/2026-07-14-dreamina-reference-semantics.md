# Dreamina Reference Semantics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prevent Dreamina from reproducing the three module contact sheets as whole frames and replace the flat camera diagram with a cinematic inside-sphere POV reference.

**Architecture:** Keep the existing six-reference workflow and filename routing. Strengthen the Clip A text contract so each reference has one explicit role, then replace only `interiorCameraSvg()` with a no-text, no-Logo, no-ring perspective composition built from blank glass quadrilaterals at three depth layers. Extend the existing Node test file so prompt semantics and SVG structure remain reproducible.

**Tech Stack:** TypeScript, Node test runner, Sharp SVG-to-PNG rendering, Markdown/plain-text prompts, ImageMagick verification.

**Workspace constraint:** Execute directly in the current `master` workspace as requested. The repository contains unrelated user changes, so every commit must use `git commit --only` with the exact task paths.

---

## File map

- Modify `docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt`: assign contact-sheet and spatial-topology roles by uploaded filename and add explicit negative constraints.
- Modify `docs/task/flyenv-dreamina-two-stage-intro/web-execution.md`: explain those roles at upload time.
- Modify `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts`: replace the flat ring diagram generated by `interiorCameraSvg()` with a three-layer perspective scene.
- Modify `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts`: add prompt-role and SVG-structure regression tests.
- Regenerate `docs/task/flyenv-dreamina-two-stage-intro/references/interior-camera.png`: produce the new 1920×1080 reference from the source generator.

### Task 1: Mark module boards as identity contact sheets

**Files:**

- Modify: `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts`
- Modify: `docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt`
- Modify: `docs/task/flyenv-dreamina-two-stage-intro/web-execution.md`

- [ ] **Step 1: Write the failing prompt-role test**

Add this test after the existing filename-reference test:

```ts
test('Clip A treats module boards as identity contact sheets rather than complete scenes', async () => {
  const prompt = await readFile(path.join(import.meta.dirname, 'clip-a-prompt-zh.txt'), 'utf8')
  assert.match(prompt, /模块身份素材索引表/)
  assert.match(prompt, /不是场景画面、构图参考、UI 面板/)
  assert.match(prompt, /24 组.*相互独立/)
  assert.match(prompt, /严禁.*整张素材表/)
  assert.match(prompt, /2×4 网格/)
})
```

- [ ] **Step 2: Run the focused test and verify the expected failure**

Run:

```bash
node --import tsx --test --test-name-pattern="identity contact sheets" docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts
```

Expected: one failed test because `clip-a-prompt-zh.txt` does not yet contain `模块身份素材索引表`.

- [ ] **Step 3: Strengthen the contact-sheet contract in Clip A**

Replace the current opening paragraph with these three exact paragraphs before the first timeline paragraph:

```text
生成严格连续、无切镜的 5 秒 16:9 电影级 3D 科技镜头，并生成同步的电子音乐与动作音效。

重要：@module-board-a.png、@module-board-b.png、@module-board-c.png 是模块身份素材索引表（contact sheet），不是场景画面、构图参考、UI 面板或能够直接进入视频的完整物体。必须逐项读取其中 24 组“官方 Logo + 精确英文名称”，把每一组作为一张相互独立的深色玻璃模块铭牌，名称必须逐字一致。严禁复制整张素材表、2×4 网格、青蓝外框、纯色底板或原始排版；任何时刻都不能让完整素材表出现在画面中，也不能让整张素材表作为一个物体从显示器飞出。

@logo-off.png 是唯一允许出现的 FlyEnv 官方 Logo。@interior-camera.png 只定义摄影机位于空心球体内部、从球心向外看的第一人称空间构图，不得复制成外部球体。
```

Replace the complete `0.6–2.4 秒` paragraph with:

```text
0.6–2.4 秒：从三张模块身份素材索引表中逐项提取的 24 张独立深色玻璃模块铭牌，从显示器屏幕连续向外喷涌；每次飞出的必须是一张独立铭牌，绝不能是整张素材表或一组固定网格。最初几张间隔明显、动作清楚，随后间隔持续缩短、速度逐渐加快，形成一次连贯的 Boom 式空间爆发。每张铭牌必须保持参考图中的官方 Logo 和对应英文名称，飞向不同 Z 轴深度，始终正对摄影机，不能变成粒子、乱码或重复牌。
```

- [ ] **Step 4: Synchronize the web upload guide**

After the five Clip A filenames, add:

```markdown
其中 `module-board-a.png`、`module-board-b.png`、`module-board-c.png` 只是模块身份素材索引表。即梦必须读取板内 24 组 Logo 和名称并分别生成独立铭牌，不能复制整张板、两列网格、外框、底板或原始排版。
```

- [ ] **Step 5: Run the focused test and verify it passes**

Run the Step 2 command again.

Expected: one passing test, zero failures.

- [ ] **Step 6: Format the changed test and guide**

```bash
npx prettier --write docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
```

Expected: exit 0 with no formatting errors.

- [ ] **Step 7: Commit the contact-sheet contract with path isolation**

```bash
git add docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
git commit --only -m "fix: classify Dreamina module contact sheets" -- docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
```

Expected: one commit containing only these three paths.

### Task 2: Replace the flat ring with a layered inside-sphere POV

**Files:**

- Modify: `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts`
- Modify: `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts`
- Regenerate: `docs/task/flyenv-dreamina-two-stage-intro/references/interior-camera.png`

- [ ] **Step 1: Write the failing SVG-structure test**

Replace the current `interior camera reference contains no text element` test with:

```ts
test('interior camera reference is a layered POV rather than a flat ring diagram', () => {
  const svg = interiorCameraSvg()
  assert.match(svg, /data-camera="inside-sphere-pov"/)
  assert.equal(svg.match(/data-depth="foreground"/g)?.length, 4)
  assert.equal(svg.match(/data-depth="middle"/g)?.length, 6)
  assert.equal(svg.match(/data-depth="far"/g)?.length, 8)
  assert.equal(svg.match(/data-blank-plaque="true"/g)?.length, 18)
  assert.doesNotMatch(svg, /<(?:ellipse|circle)\b/)
  assert.doesNotMatch(svg, /<(?:text|image)\b/)
})
```

- [ ] **Step 2: Run the focused test and verify the expected failure**

Run:

```bash
node --import tsx --test --test-name-pattern="layered POV" docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts
```

Expected: failure because the current SVG has `data-camera="inside-sphere"`, two ellipses, multiple circles, and no depth tags.

- [ ] **Step 3: Replace `interiorCameraSvg()` with depth-layered blank plaques**

Add these definitions immediately above `interiorCameraSvg()`:

```ts
type InteriorDepth = 'far' | 'middle' | 'foreground'

interface InteriorPlaque {
  depth: InteriorDepth
  path: string
}

const INTERIOR_PLAQUES: readonly InteriorPlaque[] = [
  { depth: 'far', path: 'M770 210 L935 195 L940 265 L765 282 Z' },
  { depth: 'far', path: 'M1025 195 L1205 215 L1210 286 L1020 270 Z' },
  { depth: 'far', path: 'M610 330 L805 300 L815 385 L600 420 Z' },
  { depth: 'far', path: 'M1110 310 L1340 345 L1328 435 L1100 398 Z' },
  { depth: 'far', path: 'M690 525 L875 510 L880 590 L680 610 Z' },
  { depth: 'far', path: 'M1030 500 L1245 525 L1238 615 L1020 585 Z' },
  { depth: 'far', path: 'M790 710 L950 700 L952 770 L785 780 Z' },
  { depth: 'far', path: 'M1010 700 L1195 715 L1190 790 L1005 770 Z' },
  { depth: 'middle', path: 'M80 80 L570 20 L610 205 L115 285 Z' },
  { depth: 'middle', path: 'M1380 25 L1860 100 L1805 290 L1340 205 Z' },
  { depth: 'middle', path: 'M20 350 L510 310 L535 525 L0 565 Z' },
  { depth: 'middle', path: 'M1420 330 L1920 390 L1920 610 L1390 535 Z' },
  { depth: 'middle', path: 'M100 720 L600 800 L560 990 L45 900 Z' },
  { depth: 'middle', path: 'M1370 790 L1835 715 L1900 910 L1410 995 Z' },
  { depth: 'foreground', path: 'M450 -180 L1470 -140 L1360 150 L560 195 Z' },
  { depth: 'foreground', path: 'M-360 120 L300 60 L450 720 L-240 850 Z' },
  { depth: 'foreground', path: 'M1620 130 L2280 210 L2130 880 L1480 720 Z' },
  { depth: 'foreground', path: 'M400 930 L1520 900 L1700 1230 L250 1260 Z' }
] as const

const INTERIOR_DEPTH_STYLE: Record<
  InteriorDepth,
  { fill: string; stroke: string; fillOpacity: number; strokeOpacity: number; strokeWidth: number }
> = {
  far: {
    fill: '#071a34',
    stroke: '#35769d',
    fillOpacity: 0.64,
    strokeOpacity: 0.62,
    strokeWidth: 3
  },
  middle: {
    fill: '#082342',
    stroke: '#4cc8e8',
    fillOpacity: 0.82,
    strokeOpacity: 0.86,
    strokeWidth: 5
  },
  foreground: {
    fill: '#0a2d50',
    stroke: '#6ce9ff',
    fillOpacity: 0.94,
    strokeOpacity: 1,
    strokeWidth: 8
  }
}
```

Replace the existing `interiorCameraSvg()` implementation with:

```ts
export function interiorCameraSvg(): string {
  const plaques = INTERIOR_PLAQUES.map((plaque, index) => {
    const style = INTERIOR_DEPTH_STYLE[plaque.depth]
    const glow = plaque.depth === 'foreground' ? ' filter="url(#glow)"' : ''
    return `<g data-plaque="${index + 1}" data-depth="${plaque.depth}" data-blank-plaque="true"${glow}>
      <path d="${plaque.path}" fill="${style.fill}" fill-opacity="${style.fillOpacity}" stroke="${style.stroke}" stroke-opacity="${style.strokeOpacity}" stroke-width="${style.strokeWidth}"/>
    </g>`
  }).join('')

  return documentSvg(`<g data-camera="inside-sphere-pov">
    <rect width="1920" height="1080" fill="url(#dome)"/>
    <path d="M960 500 L40 0 L0 0 L0 1080 L80 1080 Z" fill="#0b3156" opacity="0.12"/>
    <path d="M960 500 L1880 0 L1920 0 L1920 1080 L1840 1080 Z" fill="#14365a" opacity="0.1"/>
    <path d="M960 480 L1320 1080 L600 1080 Z" fill="#071d38" opacity="0.2"/>
    ${plaques}
  </g>`)
}
```

The far plaques are rendered first, then middle plaques, then foreground plaques, so overlap itself reinforces depth. The four foreground quadrilaterals extend beyond the 1920×1080 frame and cannot be read as a complete ring.

- [ ] **Step 4: Run the focused test and verify it passes**

Run the Step 2 command again.

Expected: one passing test, zero failures.

- [ ] **Step 5: Regenerate the six deterministic references**

Run:

```bash
node --import tsx docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts
```

Expected: command exits 0. Only `references/interior-camera.png` changes because the other five SVG sources are unchanged.

- [ ] **Step 6: Verify image dimensions and inspect the rendered POV**

Run:

```bash
magick identify -format '%f %m %wx%h %b\n' docs/task/flyenv-dreamina-two-stage-intro/references/interior-camera.png
```

Expected: `interior-camera.png PNG 1920x1080 ...`.

Open the PNG with the local image viewer and confirm all of the following: no ellipse/circle/sphere outline, no text or Logo, four cropped oversized foreground plaques, six middle plaques, eight smaller far plaques, visible overlap, and no complete external sphere.

- [ ] **Step 7: Format the changed TypeScript files**

```bash
npx prettier --write docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts
```

Expected: exit 0 with no formatting errors.

- [ ] **Step 8: Run the focused test again after formatting**

Run the Step 2 command again.

Expected: one passing test, zero failures.

- [ ] **Step 9: Commit the new perspective reference with path isolation**

```bash
git add docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts docs/task/flyenv-dreamina-two-stage-intro/references/interior-camera.png
git commit --only -m "fix: add depth to Dreamina interior reference" -- docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts docs/task/flyenv-dreamina-two-stage-intro/references/interior-camera.png
```

Expected: one commit containing only these three paths.

### Task 3: Define camera-reference semantics and 120-degree parallax

**Files:**

- Modify: `docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts`
- Modify: `docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt`
- Modify: `docs/task/flyenv-dreamina-two-stage-intro/web-execution.md`

- [ ] **Step 1: Write the failing camera-role test**

Add this test after the contact-sheet test:

```ts
test('Clip A treats the interior image as topology only and defines rotational parallax', async () => {
  const prompt = await readFile(path.join(import.meta.dirname, 'clip-a-prompt-zh.txt'), 'utf8')
  assert.match(prompt, /空间关系示意图/)
  assert.match(prompt, /不是最终美术画面/)
  assert.match(prompt, /禁止复制.*椭圆.*二维环形排布/)
  assert.match(prompt, /近景.*画面边缘/)
  assert.match(prompt, /中景.*完整可读/)
  assert.match(prompt, /远景.*轮廓清楚/)
  assert.match(prompt, /原地旋转扫描约 120 度/)
})
```

- [ ] **Step 2: Run the focused test and verify the expected failure**

Run:

```bash
node --import tsx --test --test-name-pattern="topology only" docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts
```

Expected: one failed test because the existing prompt does not call the file a `空间关系示意图` and does not specify the 120-degree layered parallax contract.

- [ ] **Step 3: Strengthen the role of `interior-camera.png` in the prompt**

Add this paragraph after the module contact-sheet contract and before the timeline:

```text
@interior-camera.png 仅用于说明摄影机与模块球体的空间关系，是空间关系示意图，不是最终美术画面。禁止复制其中任何示意几何、椭圆线框、圆圈、球体外轮廓、占位铭牌、二维环形排布或扁平信息图风格。实际画面必须是电影级三维空间：摄影机位于空心模块球体内部中心，从球心朝内壁向外看；近景铭牌巨大并从画面边缘裁切掠过，中景铭牌完整可读，远景铭牌缩小但轮廓清楚，三层之间存在明显遮挡、透视收缩和速度不同的运动视差。
```

Replace the `2.4–3.9 秒` paragraph with:

```text
2.4–3.9 秒：摄影机正面穿过显示器屏幕，进入空心三维模块球体的中心。此时是第一人称 POV：摄影机固定在球心，镜头朝球体内壁向外看，显示器已经位于摄影机身后并完全不可见。摄影机在球心原地旋转扫描约 120 度，不沿外部轨道移动；近景铭牌贴近镜头并快速从画面边缘掠过，中景铭牌保持完整可读，远景铭牌移动较慢但轮廓清楚。前景遮挡中景，中景遮挡远景，产生强烈近大远小、Z 轴纵深和多层空间视差。绝对禁止从球体外部观察、绕着显示器或完整球体转圈、把球体当成画面中的完整物体、使用二维圆环或平面图标墙。
```

- [ ] **Step 4: Synchronize the camera role in the web guide**

Append this paragraph after the module-board note added in Task 1:

```markdown
`interior-camera.png` 只是球内 POV 的空间关系示意图，不是最终画面。不要让即梦复制参考图中的占位铭牌或示意造型；最终画面应使用模块板中的真实模块身份，并表现近景裁切、中景可读、远景缩小和球心原地旋转约 120 度的多层视差。
```

- [ ] **Step 5: Run the focused test and verify it passes**

Run the Step 2 command again.

Expected: one passing test, zero failures.

- [ ] **Step 6: Format the changed test and guide**

```bash
npx prettier --write docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
```

Expected: exit 0 with no formatting errors.

- [ ] **Step 7: Run the focused test again after formatting**

Run the Step 2 command again.

Expected: one passing test, zero failures.

- [ ] **Step 8: Commit the camera-role contract with path isolation**

```bash
git add docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
git commit --only -m "fix: clarify Dreamina interior camera semantics" -- docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/clip-a-prompt-zh.txt docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
```

Expected: one commit containing only these three paths.

### Task 4: Final verification

**Files:**

- Verify: `docs/task/flyenv-dreamina-two-stage-intro/`

- [ ] **Step 1: Check formatting for the changed text and TypeScript files**

```bash
npx prettier --check docs/task/flyenv-dreamina-two-stage-intro/reference-assets.ts docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts docs/task/flyenv-dreamina-two-stage-intro/web-execution.md
```

Expected: exit 0 with no formatting errors.

- [ ] **Step 2: Run the complete reference suite**

```bash
node --import tsx --test docs/task/flyenv-dreamina-two-stage-intro/reference-assets.test.ts
```

Expected: 14 tests pass, 0 fail.

- [ ] **Step 3: Verify reference dimensions**

```bash
magick identify -format '%f %m %wx%h %b\n' docs/task/flyenv-dreamina-two-stage-intro/references/*.png
```

Expected: all six files are PNG and 1920×1080.

- [ ] **Step 4: Scan for stale numbered-image references and diff errors**

```bash
rg -n "@图片[0-9]+|图片[[:space:]]+[0-9]+" docs/task/flyenv-dreamina-two-stage-intro
git diff --check -- docs/task/flyenv-dreamina-two-stage-intro
```

Expected: `rg` finds no numbered-image placeholders; `git diff --check` exits 0 with no output.

- [ ] **Step 5: Confirm task-path cleanliness and recent commits**

```bash
git status --short -- docs/task/flyenv-dreamina-two-stage-intro
git log -3 --oneline -- docs/task/flyenv-dreamina-two-stage-intro
```

Expected: the scoped status is empty and the three implementation commits are the newest task-path commits. Unrelated repository changes remain untouched.
