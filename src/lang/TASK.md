# 多语言差异修复任务

## 本任务目标:
1. 移除未使用的键
2. 修复各个语言包的差异, 补全各个语言缺少的键

## 执行流程
1. 执行此目录下的 `check.mjs` 文件, 会返回未使用的键和各个语言包缺少的键
2. **严格按照第一步返回的结果**, 移除未使用的键, 补全各个语言缺少的键. **严禁删除任何语言文件**
3. 再次执行 `check.mjs` 文件, 验证结果
