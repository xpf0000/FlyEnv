// Reuse FlyEnv's real Mailpit backend implementation.
// plugin:build bundles the referenced FlyEnv fork sources into the standalone plugin artifact.
import MailPit from '@fork/module/MailPit'

export default MailPit
